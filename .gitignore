.venv/
__pycache__/
*.pyc
*.pyo
.DS_Store
.ipynb_checkpoints/
.env

# Raw filings are large (~3MB each) and reproducible via fetch_10k.py.
# Manifests are kept for metadata.
data/raw/*.html
data/raw/*.htm

# Local Claude Code config (user-specific permissions)
.claude/

# Internal resume-agent ingestion notes (not for public consumption)
RESUME_NOTES.md
